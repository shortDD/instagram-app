import React from "react";
import { Text } from "react-native";
import { Container } from "../screens/BottomTab/Home";

const NoData = () => {
  return (
    <Container>
      <Text>No Data</Text>
    </Container>
  );
};

export default NoData;
